import RevenueCatPrototypeClient from './RevenueCatPrototypeClient';

export const metadata = {
  title: 'Supporter Membership — Heart of Living Yoga',
  robots: { index: false, follow: false },
};

export default function RevenueCatPrototypePage() {
  return <RevenueCatPrototypeClient />;
}
