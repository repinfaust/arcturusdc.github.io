import MediaPrototypeClient from './MediaPrototypeClient';

export const metadata = {
  title: 'Practice Library Upload — Heart of Living Yoga',
  robots: { index: false, follow: false },
};

export default function MediaPrototypePage() {
  return <MediaPrototypeClient />;
}
