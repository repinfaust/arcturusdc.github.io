import DialledPromoCampaignClient from './DialledPromoCampaignClient';

export const metadata = {
  title: 'Promo campaigns — Dialled MTB',
  robots: { index: false, follow: false },
};

export default function DialledPromoCampaignPage() {
  return <DialledPromoCampaignClient />;
}
