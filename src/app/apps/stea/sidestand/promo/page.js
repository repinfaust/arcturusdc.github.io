import SidestandPromoClient from './SidestandPromoClient';

export const metadata = { title: 'Promo campaigns — Sidestand', robots: { index: false, follow: false } };

export default function SidestandPromoPage() {
  return <SidestandPromoClient />;
}
