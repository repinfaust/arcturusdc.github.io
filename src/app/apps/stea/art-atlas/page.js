import ArtAtlasClient from './ArtAtlasClient';

export const metadata = {
  title: 'Art Atlas | STEa',
  description: 'A source-attributed constellation timeline and walkable art-history museum.',
};

export default function ArtAtlasPage() {
  return <ArtAtlasClient />;
}
